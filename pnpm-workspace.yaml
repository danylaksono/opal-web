allowBuilds:
  esbuild: set this to true or false
# pnpm 11 reads settings from here rather than package.json.
# esbuild ships a prebuilt binary that its install script places; without this
# vite and tsx fail to start.
onlyBuiltDependencies:
  - esbuild
