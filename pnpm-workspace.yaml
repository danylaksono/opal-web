# pnpm 11 reads settings from here rather than package.json, and asks per
# dependency whether its install scripts may run. esbuild's script places a
# prebuilt binary; without it vite and tsx do not start.
allowBuilds:
  esbuild: true
onlyBuiltDependencies:
  - esbuild
